# Диапазоны, а не жёсткие версии: на сервере уже может стоять что-то новее,
# и переустановка не должна ничего понижать.
Django>=5.2,<7.0
requests>=2.32
python-dotenv>=1.0
gunicorn>=23.0

# PostgreSQL. Ставится по необходимости: без DATABASE_URL проект живёт
# на SQLite, и драйвер ему не нужен — на машине разработчика тоже.
# Скрипт scripts/to-postgres.sh ставит его сам при переносе.
# psycopg[binary]>=3.1
